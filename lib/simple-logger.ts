export function recordUsageCost(_params: {
  section: string
  model: string
  costUnits: number
  countCall?: boolean
}): void {
  // Billing stays in Doctor Opus. This app only shows the running rate.
}
